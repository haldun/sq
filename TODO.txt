- how about number of AST tokens and complexity metrics?

- how about generics?

- handle scoped imports e.g. `import struct Foundation.URL`

- separate superclass from protocol conformances for classes. SwiftSyntax cannot distinguish between them without type resolution so
both end up in conformances. SourceKit would be needed for accuracy.
